#include <stdio.h>
#include "./max.h"
#include "./min.h>"
#include "./odd.h>"
#define len 3

int arr[len] {3,1,10};

int main(){
    int indexMax = max(arr);
    /// otras llamadas;
    printf("max value: %d\n", arr[indexMax]);
    int a = 5;

    return 0;
}